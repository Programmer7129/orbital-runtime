"""orbital-runtime: radiation fault-tolerance runtime for COTS GPUs in orbit.

M0/M1 surface: orbit environment model (orbit/), fault injectors (inject/),
CLI runner (run.py) and JSONL telemetry (telemetry.py).
"""

__version__ = "0.1.0"
